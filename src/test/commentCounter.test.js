import { describe, expect } from '@jest/globals';

describe('Comment Counter Testing', () => {
  test('Counting number of items when there is only a message of no comments', () => {
    const modalHtmlMockUp = 
  });
});